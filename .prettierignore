.agent-runs/
dist/
node_modules/
*.generated.ts
*.generated.css
examples/*.html
.big-plan/*.html

# Review and feedback state the running product writes beside a plan. Git
# ignores it as never-shared local state, so formatting it only fails lint for
# whoever has run the product here.
**/.big-plan/feedback/
**/.big-plan/review/

data/**/*.html
docs/public/demo/
playwright-report/
test-results/
bun.lock
.copia/
.astro/

# MDX plan documents are product input with author-controlled layout
# (one sentence per line); Prettier only speaks legacy MDX v1 anyway.
*.mdx
.claude/
