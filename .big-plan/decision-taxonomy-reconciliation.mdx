# Decision family reconciliation

The approved interaction design should survive, but the public components should be named for the reader jobs they perform rather than the arrangements they render.

<QuickSummary>

<Why>

- `Decision` is settled; the remaining design choice is whether analysis stays an audit record or also becomes a weighted chooser.

</Why>

<What>

- Keep `Decision`, compare two live `DecisionAnalysis` behaviors, and replace the batch with a per-call `QuickDecision`.

</What>

<How>

- Show both analysis states live, then reuse the approved brief without its comparison expander for quick calls.

</How>

</QuickSummary>

<TableOfContents>

<Entry section="Recommendation" gist="Decision is fixed, QuickDecision is fixed, analysis has one open behavior choice" />

<Entry section="Captain verdict" gist="Accept the per-call quick format and make the remaining tradeoff explicit" />

<Entry section="Decision" gist="Ask and confirm one explicit answer with approved rows" />

<Entry section="DecisionAnalysis variations" gist="Compare audit-only against enabled weighted selection" />

<Entry section="QuickDecision" gist="Use the approved brief interaction without Compare all three" />

<Entry section="Boundaries and migration" gist="Make the tradeoff with round 2 explicit" />

</TableOfContents>

<Part title="Recommendation" />

## Recommendation

_Two public contracts are now settled; one behavior toggle remains for the analysis component._

| Public component | Reader job | Approved form |
| --- | --- | --- |
| `Decision` | Ask for and confirm one answer | 2.1 option rows |
| `DecisionAnalysis` | Review a scored recommendation | 3.1 keyed matrix, interaction to finalize |
| `QuickDecision` | Resolve one low-cost call | 4.1 brief minus comparison |

`DecisionMatrix` and `DecisionBrief` remain internal presentation descriptions, not public roots. `QuickDecision` is an intentional exception to the taxonomy report's warning about relative-weight names: the captain prefers one self-contained brief call over a batch, and the narrow contract makes “quick” enforceable rather than subjective prose.

The recommendation is to keep `DecisionAnalysis` audit-only by default because that preserves the round-2 ask-versus-record seam. The enabled variation is still rendered as a real alternative because choosing it would be coherent: the score grammar remains identical, and only the reader interaction changes.

<Part title="Captain verdict" />

## Captain verdict

_The family now follows the captain's chosen reading experiences while keeping the one unresolved boundary visible._

- **`Decision` — approved as-is.** Rows remain its one public presentation.
- **`DecisionAnalysis` — approved visual form, behavior still open.** Both audit-only and enabled selection appear below using the keyed matrix.
- **`QuickDecision` — one brief call, not a set.** It keeps recommendation framing, radio options, proposal, confirmation, and Change; it removes comparison entirely.
- **`DecisionMatrix`, `DecisionBrief`, and `useComplexMode` — still rejected as public API.** The first two name presentations, while the boolean would change an entire grammar.

### Why `QuickDecision` now wins

The taxonomy census still matters: six sets contain 25 questions, and twelve of those questions are too heavy to remain quick. The captain's choice accepts more slides or sub-slides for the genuinely small calls in exchange for a complete interaction around each one. That cost is explicit rather than denied.

The guardrail is now stronger: a `QuickDecision` must fit one framing sentence and short option labels. Anything needing criteria, score reasons, reversibility, or detailed option prose moves to `Decision` or `DecisionAnalysis`.

### Why analysis interaction is not `useComplexMode`

Both `DecisionAnalysis` variations author the same criteria, scores, reasons, reversibility, and options. An `interaction="audit" | "choose"` prop changes only whether the reader can select and confirm an outcome. That is a defensible behavior seam, unlike `useComplexMode`, which would switch the entire child grammar.

<Part title="Decision" />

## Decision

_Ask the reviewer to choose one answer, explain each option in place, and end with one confirmation._

### Public contract

- **`Decision`** — `question: string` is required.
- **`Option`** — `title: string` is required and unique; `recommended?: boolean` marks at most one; `summary?: string` adds one-sentence framing.
- **`Consideration`** — optional and option-local; `label: string` and `verdict: string` are required, with an optional one-sentence body.

`Decision` has no `layout`, `status`, `chosen`, `Criterion`, `Score`, or `useComplexMode` prop. The taxonomy report proposed deleting structured considerations entirely; this plan instead keeps a deliberately weaker, option-local form because the captain-approved row treatment depends on concise labels and values. There is no global criterion declaration, completeness rule, tone, or cross-option alignment. Once comparable scoring is the value, the content belongs in `DecisionAnalysis`.

### Live target appearance

_This live example uses today's `Decision` renderer with `layout="rows"` as a visual proxy. The recommended contract removes that layout prop because rows become the component's one presentation._

<Decision question="What should a maximized figure cover?" layout="rows">

<Criterion title="Comment while maximized">

Whether the reviewer can keep using the Feedback tray while the figure is enlarged.

</Criterion>
<Criterion title="Context preserved">

How much of the surrounding review document remains visible.

</Criterion>
<Criterion title="Build effort">

The relative implementation and maintenance cost.

</Criterion>

<Option title="Keep feedback tray visible" recommended summary="The figure fills the reading area; the Feedback tray stays where it is.">
<Consideration criterion="Comment while maximized" verdict="Yes" tone="good">

The tray remains visible and interactive beside the enlarged figure.

</Consideration>
<Consideration criterion="Context preserved" verdict="Strong" tone="good">

Only the reading area expands, so the application context remains intact.

</Consideration>
<Consideration criterion="Build effort" verdict="Medium" tone="mixed">

The overlay and tray must coordinate layering and keyboard focus.

</Consideration>
</Option>

<Option title="Cover the entire app" summary="The figure takes the whole window, tray included.">
<Consideration criterion="Comment while maximized" verdict="No" tone="bad">

The full-window overlay hides the existing Feedback tray.

</Consideration>
<Consideration criterion="Context preserved" verdict="Moderate" tone="mixed">

The document is obscured while the application shell remains conceptually present.

</Consideration>
<Consideration criterion="Build effort" verdict="Low" tone="good">

One overlay owns the whole window and its focus trap.

</Consideration>
</Option>

<Option title="Open a dedicated figure view" summary="The reader leaves the document for a surface built around the figure.">
<Consideration criterion="Comment while maximized" verdict="Possible" tone="mixed">

A separate view could add its own commenting controls.

</Consideration>
<Consideration criterion="Context preserved" verdict="Weak" tone="bad">

The reader leaves the plan to inspect the figure.

</Consideration>
<Consideration criterion="Build effort" verdict="High" tone="bad">

A new route needs navigation, state transfer, and its own review controls.

</Consideration>
</Option>

</Decision>

<Part title="DecisionAnalysis" />

## DecisionAnalysis variations

_The scored evidence is identical in both variations; the captain is choosing whether the reviewer audits it or can settle it in place._

### Public contract

- **`DecisionAnalysis`** — `question: string`, `state: "proposed" | "decided" | "deferred"`, and `interaction: "audit" | "choose"` are required.
- **`Criterion`** — `title: string` is required and unique; the body is a required one-sentence meaning.
- **`Option`** — `title: string` is required and unique; `recommended?: boolean`, `chosen?: boolean`, and `summary?: string` carry recommendation, outcome, and synthesis.
- **`Score`** — `criterion: string`, `verdict: string`, and `tone: "good" | "mixed" | "bad" | "neutral"` are required; the body is a one-sentence reason.
- **`Reversibility`** — `rating: "easy" | "somewhat-hard" | "hard"` and a concise body describe the cost of changing course.
- **`Details`** — optional body for deeper analysis.

Every option supplies one score for every criterion. There is no `layout` or `useComplexMode` prop, and there is no criteria-free fallback. `interaction="audit"` never renders answer controls. `interaction="choose"` is valid only for a proposed analysis; confirmation records a reading-session answer without rewriting the authored plan.

### Variation A — audit only

_Recommended. This uses today's settled `Decision` matrix to render the approved keyed rail, spacing, and definitions. The target replaces disabled radio circles with outcome markers and remains a record, not a form._

<Decision question="What should a maximized figure cover?" layout="matrix" status="decided">

<Criterion title="Comment while maximized">

Whether the reviewer can keep using the Feedback tray while the figure is enlarged.

</Criterion>
<Criterion title="Context preserved">

How much of the surrounding review document remains visible.

</Criterion>
<Criterion title="Build effort">

The relative implementation and maintenance cost.

</Criterion>

<Option title="Keep feedback tray visible" recommended chosen summary="The figure fills the reading area; the Feedback tray stays where it is.">
<Consideration criterion="Comment while maximized" verdict="Yes" tone="good">

The tray remains visible and interactive beside the enlarged figure.

</Consideration>
<Consideration criterion="Context preserved" verdict="Strong" tone="good">

Only the reading area expands, so the application context remains intact.

</Consideration>
<Consideration criterion="Build effort" verdict="Medium" tone="mixed">

The overlay and tray must coordinate layering and keyboard focus.

</Consideration>
</Option>

<Option title="Cover the entire app" summary="The figure takes the whole window, tray included.">
<Consideration criterion="Comment while maximized" verdict="No" tone="bad">

The full-window overlay hides the existing Feedback tray.

</Consideration>
<Consideration criterion="Context preserved" verdict="Moderate" tone="mixed">

The document is obscured while the application shell remains conceptually present.

</Consideration>
<Consideration criterion="Build effort" verdict="Low" tone="good">

One overlay owns the whole window and its focus trap.

</Consideration>
</Option>

<Option title="Open a dedicated figure view" summary="The reader leaves the document for a surface built around the figure.">
<Consideration criterion="Comment while maximized" verdict="Possible" tone="mixed">

A separate view could add its own commenting controls.

</Consideration>
<Consideration criterion="Context preserved" verdict="Weak" tone="bad">

The reader leaves the plan to inspect the figure.

</Consideration>
<Consideration criterion="Build effort" verdict="High" tone="bad">

A new route needs navigation, state transfer, and its own review controls.

</Consideration>
</Option>

</Decision>

The dashed underline remains the definition affordance. A criterion reveals what it means; a score reveals why that value holds. Each reveal stays one sentence maximum on hover, keyboard focus, or tap.

### Variation B — interactive weighted choice

_This variation intentionally reopens the round-2 boundary. The keyed matrix is enabled: select an option, confirm it, then use Change to reopen the same scored comparison._

<Decision question="Which maximized-figure approach should the plan use?" layout="matrix">

<Criterion title="Comment while maximized">

Whether the reviewer can keep using the Feedback tray while the figure is enlarged.

</Criterion>
<Criterion title="Context preserved">

How much of the surrounding review document remains visible.

</Criterion>
<Criterion title="Build effort">

The relative implementation and maintenance cost.

</Criterion>

<Option title="Keep feedback tray visible" recommended summary="The figure fills the reading area; the Feedback tray stays where it is.">
<Consideration criterion="Comment while maximized" verdict="Yes" tone="good">

The tray remains visible and interactive beside the enlarged figure.

</Consideration>
<Consideration criterion="Context preserved" verdict="Strong" tone="good">

Only the reading area expands, so the application context remains intact.

</Consideration>
<Consideration criterion="Build effort" verdict="Medium" tone="mixed">

The overlay and tray must coordinate layering and keyboard focus.

</Consideration>
</Option>

<Option title="Cover the entire app" summary="The figure takes the whole window, tray included.">
<Consideration criterion="Comment while maximized" verdict="No" tone="bad">

The full-window overlay hides the existing Feedback tray.

</Consideration>
<Consideration criterion="Context preserved" verdict="Moderate" tone="mixed">

The document is obscured while the application shell remains conceptually present.

</Consideration>
<Consideration criterion="Build effort" verdict="Low" tone="good">

One overlay owns the whole window and its focus trap.

</Consideration>
</Option>

<Option title="Open a dedicated figure view" summary="The reader leaves the document for a surface built around the figure.">
<Consideration criterion="Comment while maximized" verdict="Possible" tone="mixed">

A separate view could add its own commenting controls.

</Consideration>
<Consideration criterion="Context preserved" verdict="Weak" tone="bad">

The reader leaves the plan to inspect the figure.

</Consideration>
<Consideration criterion="Build effort" verdict="High" tone="bad">

A new route needs navigation, state transfer, and its own review controls.

</Consideration>
</Option>

</Decision>

Both variations preserve the same one-sentence definition contract. The difference is deliberately limited to answer behavior:

- **Audit only:** strongest round-2 and taxonomy fit; plan acceptance or feedback settles disagreement outside the matrix.
- **Interactive:** fastest for a captain who wants to settle a weighted call in place; `DecisionAnalysis` now both explains and asks.

<Part title="QuickDecision" />

## QuickDecision

_Give one small call the approved brief interaction, with no comparison disclosure hiding underneath it._

### Public contract

- **`QuickDecision`** — `question: string` is required; `context?: string` is one optional framing sentence.
- **`Option`** — `title: string` is required and unique within the question; `recommended?: boolean` marks at most one; `summary?: string` is one optional sentence.

`QuickDecision` has no set wrapper, criteria, scores, lifecycle state, reversibility, rich option bodies, or comparison expander. It owns native radio selection, Propose another approach, Confirm, the recorded answer, and Change.

### Live target appearance

_This uses today's `Decision` brief as a live proxy. The one non-discriminating compatibility criterion is invisible and exists only because today's compiler still requires one; the target `QuickDecision` contract has no criteria and never renders Compare all three._

<Decision question="Should the first release ship behind a feature flag?" layout="brief">

<Criterion title="Needs deeper comparison">

Whether this quick call needs a structured tradeoff matrix.

</Criterion>

<Option title="Yes, default off" recommended summary="Ship behind a feature flag, default it off, and expand only after the canary is healthy.">
<Consideration criterion="Needs deeper comparison" verdict="No" tone="neutral">

The safe rollout fits in the recommendation sentence without a score matrix.

</Consideration>
</Option>

<Option title="Yes, default on" summary="Keep an emergency off switch while exposing the release immediately.">
<Consideration criterion="Needs deeper comparison" verdict="No" tone="neutral">

The exposure difference is obvious from the option label and one-sentence framing.

</Consideration>
</Option>

<Option title="No, release directly" summary="Use the normal deployment rollback instead of a runtime gate.">
<Consideration criterion="Needs deeper comparison" verdict="No" tone="neutral">

The direct-release tradeoff is small enough to explain without weighted scores.

</Consideration>
</Option>

</Decision>

This is not `DecisionBrief` as a public root. The component is named `QuickDecision` because its contract enforces the job: one low-cost call, one sentence of framing, and no deeper comparison path.

<Part title="Boundaries" />

## Boundaries and migration

_Decision and QuickDecision are settled; implementation waits only on the analysis interaction choice._

### Reconciliation with round 2

- **Preserved in both variations:** `Decision` remains the ordinary answer component; `DecisionAnalysis` always requires complete scored evidence.
- **Preserved:** shared implementation primitives are allowed, but they do not define the product taxonomy.
- **Variation A:** preserves the ruling exactly—`Decision` asks and `DecisionAnalysis` records.
- **Variation B:** deliberately relaxes the ruling—`DecisionAnalysis` may ask only after the author has supplied the full analysis grammar.
- **Preserved visually:** option identity stays in the full-title keyed rail; criteria and values retain one-sentence dashed-underline definitions.

The interactive variation no longer requires one universal `Decision`: the two components keep different authoring grammars, while `interaction="choose"` changes only the analysis component's answer behavior. The tradeoff is conceptual, not structural—`DecisionAnalysis` would own both “audit” and “settle this scored call.”

`QuickDecision` is a separate, intentionally narrow answer component. It accepts the taxonomy cost of more slides and a relative-weight name in exchange for the captain-approved brief interaction on every small call.

### Internal primitives

- A selection primitive supports the answer controls used by `Decision`, `QuickDecision`, and interactive `DecisionAnalysis`.
- A keyed identity rail and comparison matrix support both `DecisionAnalysis` variations.
- The brief choice list, proposal, confirmation, recorded answer, and Change behavior are shared by `QuickDecision` without carrying over its comparison disclosure.
- Definition popovers remain shared presentation infrastructure for criterion meanings and score reasons.

These are internal seams. They should be extracted only where there are real consumers, and none should force public components to share a grammar.

### Pre-release migration

1. Make current `Decision` rows its only public presentation and remove `layout`, lifecycle, and matrix-only authoring from its contract.
2. Rename and sharpen `ComplexDecision` to `DecisionAnalysis`; require criteria, complete score coverage, state, and reversibility, then implement the selected interaction variation.
3. Replace `SimpleDecisionSet` with standalone `QuickDecision`; migrate each genuinely small question to one brief-minus-compare call and promote the heavier twelve observed questions.
4. Retire public `brief` and `matrix` layouts. Carry their approved visual language into `QuickDecision` and `DecisionAnalysis` respectively.
5. Update every example, test, machine model, and authoring guide as one clean pre-release contract change; add no compatibility aliases.

### Acceptance boundary

`Decision` and `QuickDecision` need no further UX choice. The captain's remaining selection is `DecisionAnalysis` Variation A (audit only, recommended) or Variation B (interactive weighted choice). This artifact uses existing renderers plus one example-enabling rule that suppresses an empty brief comparison; it does not implement the renamed public components.
