# Big Plan documentation site

See the repository root `AGENTS.md` and `README.md` for project guidance and development commands.

This directory owns the Astro and Starlight documentation site.
The logo files here are copies whose source of truth is the repository root `assets/` directory.
The docs favicon (`public/favicon.svg`) is a deliberate variant: the root favicon marks inside a box, so browser tabs distinguish the docs site from documents rendered by the tool itself.
