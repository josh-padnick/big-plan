// Generated by scripts/gen-guidance.mjs from assets/guidance/ and
// src/components/*/*.guidance.md. Do not edit.

export const GUIDANCE_MARKDOWN = "# How to write a plan a human loves to review\n\nYou are writing for one reader: a human deciding whether to let you build.\nThey read top to bottom, orienting first and verifying second, and every sentence costs them time.\nGet to the point, and stay there.\n\n## 1. Name the outcome, then state the thesis\n\nThe title names what will exist after execution, in a punchy noun phrase of at most eight words.\nWrite \"Ship the official Big Plan skill\", never \"Skill implementation plan\" or a title so long it reads as a sentence.\nFollow the title immediately with a lede: exactly one concise sentence, reading as the document's subtitle.\nPrefer an imperative mission statement: \"Create a version-controlled skill file and make it easily installable.\"\nBig Plan renders that first paragraph as the subtitle, so keep it as short as it can be while still carrying the thesis.\nUse plain language.\nNever open the lede with \"I propose\", \"This plan\", or any other words about the document or its author; state the mission, not the act of proposing it.\nNever use a concept the reader has not met yet; a coined phrase like a hyphen-chained workflow name belongs after the section that introduces it, if anywhere.\n\n## 2. Open with a quick summary\n\nPut a `QuickSummary` component directly after the lede, before any section: the plan's key points, stated as concisely as possible.\nIts `Why`, `What`, and `How` sections tell a stopping reader the business value, the change, and the actions you will take.\nIt enforces its own shape - short bullet lists within hard caps - and `big-plan guidance QuickSummary` explains how to use it well.\nEverything after the quick summary is elaboration; nothing essential may appear for the first time in a later section.\n\n## 3. Choose the structure this plan needs\n\nThere is no canonical section list; use the sections this plan needs, and only those.\nOrder them by the reader's questions: what is true today, what the delivered result will be like, how it works in detail, and how you will both know it is done.\nKeep orientation ahead of detail; if a section would make the reader ask \"why am I reading this now?\", it is in the wrong place.\nFold \"why X rather than Y\" justifications into the surrounding story or a decision component, never into free-floating essay sections.\nPresent delivery logistics, such as PR sequencing, as supporting decisions rather than headlines.\n\n## 4. Be terse\n\nWrite as tersely as the content allows.\nPrefer one precise sentence over three cautious ones, and cut anything that restates another section, narrates your process, or exists to look thorough.\nKeep paragraphs short; a reviewer should never lose the argument inside a wall of text.\n\n## 5. Say \"Acceptance criteria\"\n\nName the verification contract \"Acceptance criteria\".\nAvoid vaguer labels such as \"Desired outcome\" or \"Definition of done\".\nPlace it near the end, after the reader understands the approach, and make every criterion independently checkable.\n\n## 6. Use components where they beat prose\n\nBig Plan ships components that present specific kinds of information better than paragraphs can:\n\n- `QuickSummary` for the key points a reviewer reads first.\n- `Decision` for a tradeoff read option by option; `ComplexDecision` for a full criteria matrix; `SimpleDecisionSet` for quick calls.\n- `CodeDiff` and `CodeSnippet` for concrete code the reviewer should see.\n- `FileTree` and `FileTreeDiff` for layout and placement changes.\n- `DatabaseTableSchema`, `HttpEndpoint`, `GraphqlOperation`, and `GrpcMethod` for structured contracts.\n- `Callout` to make one decision, warning, or note impossible to miss.\n\nA decision buried in prose is a decision the reviewer cannot easily accept or reject.\nBefore using a component, run `big-plan guidance <Component>` (for example `big-plan guidance ComplexDecision`) for how to use it well.\n\n## 7. Validate, render, and reread\n\nRun `big-plan validate <plan.mdx>` and fix every diagnostic until it passes.\nLint catches what is statically analyzable; it cannot judge writing.\nRender the plan, then reread the rendered document exactly as the human will, top to bottom.\nIf any section reads awkwardly, or the opening does not orient you, revise before presenting it.\n";

export const COMPONENT_GUIDANCE: Readonly<Record<string, string>> = {
  "Callout": "# Using Callout well\n\nAn accent-bordered panel for the one thing a skimming reviewer must not miss.\n\n- Choose the type by consequence: `note` for context, `tip` for a better path, `warning` for risk, `danger` for harm.\n- A callout interrupts reading; use one only when the interruption is the point.\n- If a document has more than a few, they stop working; demote ordinary detail back to prose.\n",
  "CodeDiff": "# Using CodeDiff well\n\nOne file's unified diff, with gutters and annotations anchored to lines.\n\n- Reach for it when the reviewer should judge a concrete change, not read a description of one.\n- Show the smallest hunk that carries the decision; a full-file dump asks the reviewer to do your editing.\n- Anchor an Annotation to any line whose reason is not obvious from the code itself.\n- In a plan, a diff is a proposal: it must match what you intend to write, or the approval is meaningless.\n",
  "CodeSnippet": "# Using CodeSnippet well\n\nAn excerpt of existing code, with optional file identity and file-absolute line numbers.\n\n- Reach for it when the reviewer needs to see current reality: the contract you will preserve or the code you will change.\n- Keep the excerpt to the lines that matter and use file-absolute numbering so the reviewer can verify it in place.\n- Use CodeDiff for proposed changes; CodeSnippet shows what is, not what will be.\n",
  "ComplexDecision": "# Using ComplexDecision well\n\nOne weighty tradeoff the reviewer must settle, with options scored against criteria and a recommendation.\n\n- Reach for it only when options genuinely need side-by-side scoring across shared criteria; the matrix is the heaviest read in Big Plan. Prefer `Decision` for most tradeoffs.\n- Give every option an honest summary and score; a card with one real option and two strawmen wastes the reviewer's trust.\n- Mark your recommendation, and keep the deciding context in the card rather than scattered through nearby sections.\n- Two or three per plan is a lot; if everything is a ComplexDecision, nothing is.\n",
  "DatabaseTableSchema": "# Using DatabaseTableSchema well\n\nOne table's schema as structured columns, keys, indexes, and titled verbatim DDL.\n\n- Reach for it whenever a plan proposes or changes a table; reviewers catch schema mistakes in structure, not in prose.\n- One component per table; a plan touching three tables shows three schemas.\n- Put constraints and indexes in the structured schema, and reserve the DDL band for what the subset cannot express, such as row security or triggers.\n",
  "Decision": "# Using Decision well\n\nThe default decision card: each option carries its own considerations inline, so a reviewer reads one option in full instead of parsing a matrix.\n\n- Reach for it for most tradeoffs; escalate to ComplexDecision only when options genuinely need side-by-side comparison across shared criteria.\n- Give every option `Consideration` lines with an honest verdict and tone; put the deciding nuance in the consideration's body.\n- Mark your recommendation, and keep option details short - the considerations are the story.\n- Use SimpleDecisionSet for quick calls that need no per-consideration reasoning.\n",
  "FileTreeDiff": "# Using FileTreeDiff well\n\nOne combined change tree with per-entry added, modified, moved, or deleted status.\n\n- Reach for it when the shape of a change across files is the story: a restructuring, extraction, or migration.\n- Every entry the plan creates, moves, or deletes belongs in the tree; an incomplete tree reads as a complete one.\n- Pair it with prose for the why; the tree carries the what and where.\n",
  "FileTree": "# Using FileTree well\n\nA plain file hierarchy with optional per-entry notes.\n\n- Reach for it when placement is part of the proposal: where new code will live, or the current layout the reviewer needs in mind.\n- Show only the subtree the plan touches; a full repository listing hides the point.\n- Add a note to any entry whose purpose the name alone does not carry.\n",
  "GraphqlOperation": "# Using GraphqlOperation well\n\nOne GraphQL query, mutation, or subscription with its input and payload shapes and executable examples.\n\n- Reach for it when a plan adds or changes an operation; show the schema-level shape the reviewer will hold you to.\n- Give a realistic executable example; toy variables hide the decisions in the shape.\n- One component per operation, and cover error payloads where the design has them.\n",
  "GrpcMethod": "# Using GrpcMethod well\n\nOne gRPC method headed by its proto signature, with message fields, status codes, and grouped examples.\n\n- Reach for it when a plan adds or changes a method; the proto signature is the contract under review.\n- List the status codes the implementation will actually return, with the condition for each.\n- One component per method, and keep field notes to what the type alone cannot say.\n",
  "HttpEndpoint": "# Using HttpEndpoint well\n\nOne HTTP endpoint's contract: parameters, request body, and status-coded responses.\n\n- Reach for it when a plan adds or changes an endpoint; the reviewer approves the contract, not a paraphrase of it.\n- Cover the failure statuses, not just the happy path; error shapes are where API reviews earn their keep.\n- One component per endpoint, and only the endpoints this plan touches.\n",
  "QuickSummary": "# Using QuickSummary well\n\nThe card a reviewer reads first: one slide that starts with the why.\nPlace it directly after the lede, before any section.\n\n- The enforced shape: `Why` and `What` (required, one bullet each), then optionally `How` (at most 3 bullets), at most 450 readable characters in total, one QuickSummary per plan.\n- `Why` states the business value at the level a sponsor would repeat; it renders as the card's hero band.\n- `What` is one complete imperative sentence naming what you will build.\n- `How` lists the actions you will take, each bullet imperative: \"Embed the skill text...\", never \"The skill is embedded...\".\n- Speak in commitments throughout, summarize at the altitude of intent, and let every dropped detail live in a section below.\n- A requirement the requester dictated is not automatically summary-worthy: unless it is the mission itself, it belongs in Acceptance criteria, where it is verified rather than repeated.\n",
  "SimpleDecisionSet": "# Using SimpleDecisionSet well\n\nA compact numbered list of quick calls the reviewer can settle in one pass.\n\n- Reach for it near the end of a plan to collect the small open questions that need answers but not tradeoff cards.\n- Each decision states its options briefly; if an option needs a paragraph of context, promote the question to a Decision.\n- Never mix settled facts into the set; it exists to gather exactly what still needs the reviewer's word.\n"
};

export const GUIDANCE_VERSION = "9e8ac2392f35";
