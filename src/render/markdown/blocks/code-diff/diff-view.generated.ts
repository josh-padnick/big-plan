// Generated by scripts/gen-browser-scripts.mjs from src/render/markdown/blocks/code-diff/diff-view.browser.ts. Do not edit.
export const DIFF_VIEW_JS = "const DIFF_VIEW_STORAGE_KEY = \"big-plan-diff-view\";\nconst DIFF_MESSAGE_RESET_MS = 2_000;\nlet nextDiffDialogLabelId = 1;\nconst diffMessageTimers = new WeakMap();\nconst isCodeDiffView = (value) => value === \"unified\" || value === \"split\";\nconst applyDiffView = ({ block, view, }) => {\n    block.dataset.diffView = view;\n    for (const button of block.querySelectorAll(\"[data-diff-set-view]\")) {\n        button.setAttribute(\"aria-pressed\", button.dataset.diffSetView === view ? \"true\" : \"false\");\n    }\n};\nconst showDiffMessage = ({ block, message, }) => {\n    const slot = block.querySelector(\"[data-diff-copy-message]\");\n    if (slot === null) {\n        return;\n    }\n    const previousTimer = diffMessageTimers.get(slot);\n    if (previousTimer !== undefined) {\n        window.clearTimeout(previousTimer);\n    }\n    const menuButton = block.querySelector(\"[data-diff-menu-button]\");\n    slot.textContent = message;\n    slot.hidden = false;\n    menuButton?.setAttribute(\"aria-label\", message);\n    const timer = window.setTimeout(() => {\n        slot.hidden = true;\n        menuButton?.setAttribute(\"aria-label\", \"More actions\");\n        diffMessageTimers.delete(slot);\n    }, DIFF_MESSAGE_RESET_MS);\n    diffMessageTimers.set(slot, timer);\n};\nconst writeDiffClipboard = async ({ container, value, }) => {\n    if (navigator.clipboard !== undefined) {\n        try {\n            await navigator.clipboard.writeText(value);\n            return;\n        }\n        catch {\n        }\n    }\n    const textarea = document.createElement(\"textarea\");\n    textarea.value = value;\n    textarea.setAttribute(\"readonly\", \"\");\n    textarea.style.position = \"fixed\";\n    textarea.style.opacity = \"0\";\n    const previousFocus = document.activeElement;\n    const activeDialog = document.querySelector(\"dialog.code-diff-dialog[open]\");\n    (activeDialog ?? container).append(textarea);\n    let copied;\n    try {\n        textarea.select();\n        copied = document.execCommand(\"copy\");\n    }\n    finally {\n        textarea.remove();\n        if (previousFocus instanceof HTMLElement) {\n            previousFocus.focus();\n        }\n    }\n    if (!copied) {\n        throw new Error(\"Clipboard copy was unavailable\");\n    }\n};\nconst updateExpandControl = ({ block, }) => {\n    const button = block.querySelector(\"[data-diff-expand]\");\n    if (button === null) {\n        return;\n    }\n    const expanded = block.dataset.diffExpanded !== undefined;\n    const label = expanded ? \"Exit full screen\" : \"View diff full screen\";\n    button.setAttribute(\"aria-label\", label);\n    button.title = label;\n    button\n        .querySelector('[data-lucide=\"maximize-2\"]')\n        ?.toggleAttribute(\"hidden\", expanded);\n    button\n        .querySelector('[data-lucide=\"minimize-2\"]')\n        ?.toggleAttribute(\"hidden\", !expanded);\n};\nconst openFullScreen = ({ block }) => {\n    const article = block.closest(\"article\");\n    const fileCaption = block.querySelector(\".code-diff-file\");\n    if (article === null || fileCaption === null) {\n        return;\n    }\n    const scrollY = window.scrollY;\n    let labelId = fileCaption.id;\n    if (labelId === \"\" || document.getElementById(labelId) !== fileCaption) {\n        do {\n            labelId = `code-diff-dialog-label-${nextDiffDialogLabelId}`;\n            nextDiffDialogLabelId += 1;\n        } while (document.getElementById(labelId) !== null);\n        fileCaption.id = labelId;\n    }\n    const placeholder = document.createElement(\"span\");\n    placeholder.hidden = true;\n    block.before(placeholder);\n    const dialog = document.createElement(\"dialog\");\n    dialog.className = \"code-diff-dialog\";\n    dialog.setAttribute(\"aria-labelledby\", labelId);\n    dialog.append(block);\n    article.append(dialog);\n    block.dataset.diffExpanded = \"\";\n    updateExpandControl({ block });\n    dialog.addEventListener(\"click\", (event) => {\n        if (event.target === dialog) {\n            dialog.close();\n        }\n    });\n    dialog.addEventListener(\"close\", () => {\n        placeholder.before(block);\n        placeholder.remove();\n        dialog.remove();\n        delete block.dataset.diffExpanded;\n        updateExpandControl({ block });\n        window.scrollTo({ top: scrollY });\n    });\n    dialog.showModal();\n};\nlet storedDiffView = \"unified\";\ntry {\n    const stored = window.localStorage.getItem(DIFF_VIEW_STORAGE_KEY);\n    if (isCodeDiffView(stored)) {\n        storedDiffView = stored;\n    }\n}\ncatch {\n}\nfor (const block of document.querySelectorAll(\"[data-code-diff]\")) {\n    const toggleGroup = block.querySelector(\"[data-diff-toggle-group]\");\n    const expand = block.querySelector(\"[data-diff-expand]\");\n    const menuButton = block.querySelector(\"[data-diff-menu-button]\");\n    const menuList = block.querySelector(\"[data-diff-menu-list]\");\n    toggleGroup?.removeAttribute(\"hidden\");\n    expand?.removeAttribute(\"hidden\");\n    menuButton?.removeAttribute(\"hidden\");\n    applyDiffView({ block, view: storedDiffView });\n    const menuItems = () => menuList === null\n        ? []\n        : [...menuList.querySelectorAll('[role=\"menuitem\"]')];\n    const setMenuOpen = ({ open, focus, }) => {\n        if (menuButton === null || menuList === null) {\n            return;\n        }\n        menuButton.setAttribute(\"aria-expanded\", open ? \"true\" : \"false\");\n        menuList.hidden = !open;\n        const items = menuItems();\n        for (const item of items) {\n            item.tabIndex = -1;\n        }\n        if (open && focus !== undefined) {\n            const item = items[focus === \"first\" ? 0 : items.length - 1];\n            if (item !== undefined) {\n                item.tabIndex = 0;\n                item.focus();\n            }\n        }\n    };\n    menuButton?.addEventListener(\"click\", () => {\n        const open = menuButton.getAttribute(\"aria-expanded\") !== \"true\";\n        setMenuOpen({ open, ...(open ? { focus: \"first\" } : {}) });\n    });\n    block\n        .querySelector(\"[data-diff-menu]\")\n        ?.addEventListener(\"keydown\", (event) => {\n        if (menuList === null) {\n            return;\n        }\n        if (event.key === \"Escape\" && !menuList.hidden) {\n            event.preventDefault();\n            event.stopPropagation();\n            setMenuOpen({ open: false });\n            menuButton?.focus();\n            return;\n        }\n        if (event.target === menuButton && event.key === \"ArrowDown\") {\n            event.preventDefault();\n            setMenuOpen({ open: true, focus: \"first\" });\n            return;\n        }\n        if (event.target === menuButton && event.key === \"ArrowUp\") {\n            event.preventDefault();\n            setMenuOpen({ open: true, focus: \"last\" });\n            return;\n        }\n        if (menuList.hidden) {\n            return;\n        }\n        if (event.key === \"Tab\") {\n            setMenuOpen({ open: false });\n            return;\n        }\n        const items = menuItems();\n        const currentIndex = items.findIndex((item) => item === event.target);\n        if (currentIndex === -1) {\n            return;\n        }\n        const destination = event.key === \"Home\"\n            ? 0\n            : event.key === \"End\"\n                ? items.length - 1\n                : event.key === \"ArrowDown\"\n                    ? (currentIndex + 1) % items.length\n                    : event.key === \"ArrowUp\"\n                        ? (currentIndex - 1 + items.length) % items.length\n                        : undefined;\n        if (destination !== undefined) {\n            event.preventDefault();\n            const item = items[destination];\n            if (item !== undefined) {\n                for (const menuItem of items) {\n                    menuItem.tabIndex = menuItem === item ? 0 : -1;\n                }\n                item.focus();\n            }\n        }\n    });\n    const copyToClipboard = async ({ value, successMessage, }) => {\n        setMenuOpen({ open: false });\n        menuButton?.focus();\n        try {\n            await writeDiffClipboard({ container: block, value });\n            showDiffMessage({ block, message: successMessage });\n        }\n        catch {\n            showDiffMessage({ block, message: \"Could not copy\" });\n        }\n    };\n    block\n        .querySelector(\"[data-diff-copy-path]\")\n        ?.addEventListener(\"click\", () => {\n        void copyToClipboard({\n            value: block.dataset.diffPath ?? \"\",\n            successMessage: \"Path copied!\",\n        });\n    });\n    block\n        .querySelector(\"[data-diff-copy]\")\n        ?.addEventListener(\"click\", () => {\n        const source = block.querySelector(\"[data-diff-source]\");\n        if (source === null) {\n            return;\n        }\n        void copyToClipboard({\n            value: source.value,\n            successMessage: \"Diff copied!\",\n        });\n    });\n    expand?.addEventListener(\"click\", () => {\n        const openDialog = block.closest(\"dialog\");\n        if (openDialog !== null) {\n            openDialog.close();\n            return;\n        }\n        openFullScreen({ block });\n    });\n    for (const button of block.querySelectorAll(\"[data-diff-set-view]\")) {\n        button.addEventListener(\"click\", () => {\n            const view = button.dataset.diffSetView ?? null;\n            if (!isCodeDiffView(view)) {\n                return;\n            }\n            applyDiffView({ block, view });\n            try {\n                window.localStorage.setItem(DIFF_VIEW_STORAGE_KEY, view);\n            }\n            catch {\n            }\n        });\n    }\n}\ndocument.addEventListener(\"click\", (event) => {\n    for (const menu of document.querySelectorAll(\"[data-diff-menu]\")) {\n        const button = menu.querySelector(\"[data-diff-menu-button]\");\n        const list = menu.querySelector(\"[data-diff-menu-list]\");\n        if (button === null || list === null || list.hidden) {\n            continue;\n        }\n        if (event.target instanceof Node && menu.contains(event.target)) {\n            continue;\n        }\n        button.setAttribute(\"aria-expanded\", \"false\");\n        list.hidden = true;\n    }\n});\n";
