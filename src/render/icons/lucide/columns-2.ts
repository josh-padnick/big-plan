// Lucide "columns-2" icon path data; named by its Lucide catalog name so the
// icon set stays globally consistent and reusable across components.

import type { IconNode } from "../lucide-icon.js";

export const COLUMNS_ICON: IconNode = [
  ["rect", { width: "18", height: "18", x: "3", y: "3", rx: "2" }],
  ["path", { d: "M12 3v18" }],
];
