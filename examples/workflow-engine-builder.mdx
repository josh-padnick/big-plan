# Workflow Builder Surface

A builder for declarative agent workflows, sketched end to end with `Wireframe` so the whole surface is arguable before any of it is built.

## What this covers

A workflow is a graph: nodes are agent steps, edges carry control and data, and a gate stops the run until a human approves.

The prototype below walks the path an author actually takes - find the workflow, open the canvas, configure a step, watch a run, and fix it.

It is a desktop web product: a durable left nav, a wide content column, and the browser's own address bar showing which route each screen is. One screen at the end is deliberately mobile, because a failed run is the thing people open on a phone.

Click through it. Every arrow in the sketch is a screen you can reach.

<Wireframe
  id="workflow-builder"
  title="Workflow builder, ten desktop screens and one mobile"
  initialScreen="home"
>
  <Screen id="home" name="Home" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics">
    <AppShell>
      <Sidebar brand="Fabrica" mode="Acme Robotics">
        <Nav label="Main">
          <NavItem label="Home" active />
          <NavItem label="Workflows" navigateTo="library" />
          <NavItem label="Runs" navigateTo="runs" />
          <NavItem label="Wiki" />
          <NavItem label="Settings" navigateTo="settings" />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader
          title="Good morning, Dana"
          description="Three workflows are in flight and one is waiting on you."
        >
          <Button
            label="New workflow"
            emphasis="primary"
            navigateTo="create-basics"
          />
        </PageHeader>
        <Row gap="md">
          <Panel eyebrow="Right now" title="In flight">
            <Metric label="Running" value="3" note="Longest: 14m" />
          </Panel>
          <Panel eyebrow="Waiting on you" title="Approvals">
            <Metric label="Gates open" value="1" note="Nightly digest, step 4" />
            <Button label="Review it" navigateTo="run-detail" />
          </Panel>
          <Panel eyebrow="Last 24h" title="Health">
            <Progress
              label="Runs succeeded"
              value="86"
              detail="31 of 36 runs"
            />
          </Panel>
        </Row>
        <Panel title="Your workflows">
          <List>
            <ListItem
              label="Nightly digest"
              meta="ran 14m ago"
              value="Awaiting approval"
            />
            <ListItem label="Ticket triage" meta="ran 2h ago" value="Succeeded" />
            <ListItem
              label="Release notes"
              meta="ran yesterday"
              value="Failed"
            />
          </List>
          <Button label="See all workflows" navigateTo="library" />
        </Panel>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="library" name="Workflow library" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics/workflows">
    <AppShell>
      <Sidebar brand="Fabrica" mode="Acme Robotics">
        <Nav label="Main">
          <NavItem label="Home" navigateTo="home" />
          <NavItem label="Workflows" active />
          <NavItem label="Runs" navigateTo="runs" />
          <NavItem label="Wiki" />
          <NavItem label="Settings" navigateTo="settings" />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader
          title="Workflows"
          description="Every workflow this org can run, and who owns it."
        >
          <Button
            label="New workflow"
            emphasis="primary"
            navigateTo="create-basics"
          />
        </PageHeader>
        <Row gap="sm" align="end">
          <TextField label="Filter" kind="search" placeholder="Filter by name" />
          <Select label="Owner" value="Anyone" />
          <Select label="Scope" value="Org" />
        </Row>
        <Panel>
          <List>
            <ListItem label="Nightly digest" meta="Org · 6 steps" value="Dana" />
            <ListItem label="Ticket triage" meta="Org · 4 steps" value="Sam" />
            <ListItem
              label="Release notes"
              meta="Org · 9 steps"
              value="Priya"
            />
            <ListItem
              label="Onboarding brief"
              meta="Workspace · 3 steps"
              value="Dana"
            />
          </List>
          <Divider label="Archived" />
          <List>
            <ListItem
              label="Weekly rollup"
              meta="Org · 5 steps"
              value="Retired"
            />
          </List>
          <Button label="Open nightly digest" navigateTo="canvas" />
        </Panel>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="canvas" name="Canvas builder" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics/workflows/nightly-digest/edit">
    <AppShell>
      <Sidebar brand="Nightly digest" mode="Draft · v7">
        <Nav label="Step palette">
          <NavItem label="Agent step" />
          <NavItem label="Approval gate" />
          <NavItem label="Fan out" />
          <NavItem label="Sub-workflow" />
        </Nav>
        <Divider />
        <Text text="Drag a block onto the canvas." role="helper" />
      </Sidebar>
      <AppContent>
        <PageHeader title="Nightly digest" badge="Draft">
          <Button label="Test run" navigateTo="run-detail" />
          <Button label="Publish" emphasis="primary" navigateTo="library" />
        </PageHeader>
        <Panel eyebrow="Canvas" title="Trigger to delivery">
          <Row gap="none" align="center">
            <Panel eyebrow="Trigger" title="Every night, 6pm">
              <Text text="Schedule" role="helper" />
            </Panel>
            <Connector direction="right" />
            <Panel eyebrow="Step 1" title="Collect artifacts">
              <Text text="Agent · Researcher" role="helper" />
            </Panel>
          </Row>
          <Connector direction="down" label="on success" />
          <Row gap="none" align="center">
            <Panel eyebrow="Step 2" title="Draft the digest">
              <Text text="Agent · Writer" role="helper" />
              <Button label="Configure" navigateTo="step-config" />
            </Panel>
            <Connector direction="right" label="then" />
            <Panel eyebrow="Step 3" title="Human approval">
              <Badge label="Gate" />
              <Text text="Blocks until approved" role="helper" />
            </Panel>
          </Row>
          <Connector direction="down" label="approved" />
          <Row gap="none" align="center">
            <Panel eyebrow="Step 4" title="Publish to wiki">
              <Text text="Agent · Publisher" role="helper" />
            </Panel>
            <Connector direction="right" />
            <Panel eyebrow="Done" title="Digest artifact">
              <Text text="One artifact per night" role="helper" />
            </Panel>
          </Row>
        </Panel>
        <Text
          text="Lo-fi sketch. Node shapes and edge routing are arguable; the step vocabulary is not."
          role="muted"
        />
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="step-config" name="Step configuration" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics/workflows/nightly-digest/steps/2">
    <AppShell>
      <Sidebar brand="Nightly digest" mode="Draft · v7">
        <Nav label="Steps">
          <NavItem label="Collect artifacts" />
          <NavItem label="Draft the digest" active />
          <NavItem label="Human approval" />
          <NavItem label="Publish to wiki" />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader
          title="Draft the digest"
          description="Step 2 of 4 · agent step"
        >
          <Button label="Back to canvas" navigateTo="canvas" />
          <Button label="Save step" emphasis="primary" navigateTo="canvas" />
        </PageHeader>
        <Row gap="md">
          <Stack gap="md">
            <Panel title="What this step does">
              <TextField
                label="Step name"
                value="Draft the digest"
                hint="Shown on the canvas and in every run."
              />
              <Select label="Agent" value="Writer" />
              <TextArea
                label="Prompt"
                value="Summarize today's artifacts for the team, grouped by product."
                hint="The agent also receives the org's context tree."
              />
            </Panel>
            <Panel title="Inputs">
              <List>
                <ListItem
                  label="artifacts"
                  meta="from step 1"
                  value="Artifact[]"
                />
                <ListItem label="org" meta="from trigger" value="Org" />
              </List>
            </Panel>
          </Stack>
          <Stack gap="md">
            <Panel title="When it fails">
              <Checkbox label="Retry failed steps" checked />
              <TextField label="Attempts" kind="number" value="3" />
              <Select label="Then" value="Stop the run" />
            </Panel>
            <Panel title="Limits">
              <TextField
                label="Cost ceiling"
                value="$2.00"
                hint="The run pauses rather than overspending."
              />
              <Switch label="Require approval before this step" />
              <Switch label="Record the tool-call trace" on />
            </Panel>
          </Stack>
        </Row>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="runs" name="Run history" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics/runs">
    <AppShell>
      <Sidebar brand="Fabrica" mode="Acme Robotics">
        <Nav label="Main">
          <NavItem label="Home" navigateTo="home" />
          <NavItem label="Workflows" navigateTo="library" />
          <NavItem label="Runs" active />
          <NavItem label="Wiki" />
          <NavItem label="Settings" navigateTo="settings" />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader
          title="Runs"
          description="Every execution, newest first."
        />
        <Row gap="md">
          <Panel eyebrow="36 runs" title="Last 24 hours">
            <Row gap="sm" align="end">
              <Select label="Status" value="Any" />
              <Select label="Workflow" value="Any" />
            </Row>
            <List>
              <ListItem
                label="#1042 Nightly digest"
                meta="14m ago"
                value="Awaiting approval"
              />
              <ListItem
                label="#1041 Ticket triage"
                meta="2h ago"
                value="Succeeded"
              />
              <ListItem
                label="#1040 Release notes"
                meta="yesterday"
                value="Failed"
              />
              <ListItem
                label="#1039 Nightly digest"
                meta="yesterday"
                value="Succeeded"
              />
            </List>
            <Button label="Open #1042" navigateTo="run-detail" />
          </Panel>
          <Panel eyebrow="Selected" title="Run #1042">
            <Text text="Pick a run to see its steps here." role="muted" />
            <Button label="See the detail view" navigateTo="run-detail" />
          </Panel>
        </Row>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="run-detail" name="Run detail" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics/runs/1042">
    <AppShell>
      <Sidebar brand="Fabrica" mode="Acme Robotics">
        <Nav label="Main">
          <NavItem label="Home" navigateTo="home" />
          <NavItem label="Workflows" navigateTo="library" />
          <NavItem label="Runs" active navigateTo="runs" />
          <NavItem label="Wiki" />
          <NavItem label="Settings" navigateTo="settings" />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader
          title="Run #1042"
          description="Nightly digest · started 14m ago"
          badge="Awaiting approval"
        >
          <Button label="Back to runs" navigateTo="runs" />
          <Button label="Approve and continue" emphasis="primary" />
          <Button label="Cancel run" emphasis="destructive" navigateTo="error" />
        </PageHeader>
        <Row gap="md">
          <Panel title="Steps">
            <List>
              <ListItem
                label="1 Collect artifacts"
                meta="1m 04s"
                value="Done"
              />
              <ListItem label="2 Draft the digest" meta="12m 41s" value="Done" />
              <ListItem
                label="3 Human approval"
                meta="waiting"
                value="You"
              />
              <ListItem label="4 Publish to wiki" meta="queued" value="Queued" />
            </List>
          </Panel>
          <Stack gap="md">
            <Panel eyebrow="Step 2" title="Draft the digest">
              <Metric label="Cost" value="$0.41" note="Ceiling $2.00" />
              <Text text="Model: sonnet · 18,402 tokens" role="helper" />
              <Divider label="Output" />
              <ImagePlaceholder label="Drafted digest preview" shape="wide" />
            </Panel>
            <Panel title="Waiting on you">
              <Text text="Approve to publish tonight's digest to the wiki." />
              <Row gap="sm">
                <Button label="Approve" emphasis="primary" />
                <Button label="Send back" navigateTo="step-config" />
              </Row>
            </Panel>
          </Stack>
        </Row>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="create-basics" name="Create workflow" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics/workflows/new">
    <AppShell>
      <Sidebar brand="Fabrica" mode="Acme Robotics">
        <Nav label="Main">
          <NavItem label="Home" navigateTo="home" />
          <NavItem label="Workflows" active navigateTo="library" />
          <NavItem label="Runs" navigateTo="runs" />
          <NavItem label="Wiki" />
          <NavItem label="Settings" navigateTo="settings" />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader title="New workflow" description="Three steps, then the canvas." />
        <Stepper>
          <Step label="Basics" state="current" />
          <Step label="Trigger" state="todo" />
          <Step label="First step" state="todo" />
        </Stepper>
        <Panel title="Basics">
          <TextField
            label="Name"
            placeholder="Nightly digest"
            hint="Shown everywhere this workflow appears."
          />
          <TextArea
            label="What it is for"
            placeholder="One or two sentences a teammate could act on."
          />
          <Select label="Scope" value="Org · Acme Robotics" />
          <Checkbox label="Start from a template" />
        </Panel>
        <Row gap="sm" justify="between">
          <Button label="Cancel" emphasis="tertiary" navigateTo="library" />
          <Button
            label="Next: trigger"
            emphasis="primary"
            navigateTo="create-trigger"
          />
        </Row>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="create-trigger" name="Create workflow, trigger" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics/workflows/new/trigger">
    <AppShell>
      <Sidebar brand="Fabrica" mode="Acme Robotics">
        <Nav label="Main">
          <NavItem label="Home" navigateTo="home" />
          <NavItem label="Workflows" active navigateTo="library" />
          <NavItem label="Runs" navigateTo="runs" />
          <NavItem label="Wiki" />
          <NavItem label="Settings" navigateTo="settings" />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader title="New workflow" description="Three steps, then the canvas." />
        <Stepper>
          <Step label="Basics" state="done" />
          <Step label="Trigger" state="current" />
          <Step label="First step" state="todo" />
        </Stepper>
        <Row gap="md">
          <Panel title="What starts a run">
            <Select label="Trigger" value="On a schedule" />
            <TextField label="Runs at" kind="text" value="18:00" />
            <Select label="Time zone" value="America/Los_Angeles" />
            <Switch label="Skip when the last run is still going" on />
          </Panel>
          <Panel eyebrow="Preview" title="This reads as">
            <Text text="Every night at 6:00 PM Pacific." />
            <Text text="Next run: today, 6:00 PM." role="helper" />
            <Divider />
            <Text
              text="A schedule trigger cannot fan out. Pick an event trigger if you need one run per item."
              role="muted"
            />
          </Panel>
        </Row>
        <Row gap="sm" justify="between">
          <Button label="Back" emphasis="tertiary" navigateTo="create-basics" />
          <Button
            label="Next: first step"
            emphasis="primary"
            navigateTo="canvas"
          />
        </Row>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="settings" name="Settings" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/acme-robotics/settings">
    <AppShell>
      <Sidebar brand="Fabrica" mode="Acme Robotics">
        <Nav label="Main">
          <NavItem label="Home" navigateTo="home" />
          <NavItem label="Workflows" navigateTo="library" />
          <NavItem label="Runs" navigateTo="runs" />
          <NavItem label="Wiki" />
          <NavItem label="Settings" active />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader title="Settings" description="Acme Robotics" />
        <Row gap="md">
          <Nav label="Settings sections">
            <NavItem label="General" active />
            <NavItem label="Team" />
            <NavItem label="Blueprints" />
            <NavItem label="Templates" />
            <NavItem label="Run limits" />
          </Nav>
          <Stack gap="md">
            <Panel title="General">
              <TextField label="Organization name" value="Acme Robotics" />
              <TextField label="Slug" value="acme-robotics" />
              <Select label="Default run scope" value="Org" />
            </Panel>
            <Panel title="Run limits">
              <TextField
                label="Monthly ceiling"
                value="$400.00"
                hint="Runs queue rather than exceed it."
              />
              <Switch label="Require approval above $5 a run" on />
              <Switch label="Let members publish workflows" />
            </Panel>
            <Panel title="Danger zone">
              <Text text="Deleting an org removes its workflows and run history." />
              <Button label="Delete organization" emphasis="destructive" />
            </Panel>
          </Stack>
        </Row>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen id="empty" name="Empty library" viewport="desktop"
    chrome="browser"
    url="app.fabrica.dev/nova-labs/workflows">
    <AppShell>
      <Sidebar brand="Fabrica" mode="Nova Labs">
        <Nav label="Main">
          <NavItem label="Home" navigateTo="home" />
          <NavItem label="Workflows" active />
          <NavItem label="Runs" navigateTo="runs" />
          <NavItem label="Wiki" />
          <NavItem label="Settings" navigateTo="settings" />
        </Nav>
      </Sidebar>
      <AppContent>
        <PageHeader title="Workflows" description="Nothing here yet." />
        <Panel>
          <Stack gap="md" align="center">
            <ImagePlaceholder label="An empty canvas" shape="wide" />
            <Heading text="No workflows in Nova Labs" level="2" />
            <Text
              text="A workflow runs a sequence of agent steps on a trigger you choose."
              role="muted"
            />
            <Row gap="sm">
              <Button
                label="New workflow"
                emphasis="primary"
                navigateTo="create-basics"
              />
              <Button label="Start from a template" navigateTo="library" />
            </Row>
          </Stack>
        </Panel>
      </AppContent>
    </AppShell>
  </Screen>

  <Screen
    id="error"
    name="Mobile only: run failed"
    viewport="mobile-portrait"
    chrome="phone"
  >
    <TopBar title="Run #1040" />
    <Stack gap="md">
      <Panel eyebrow="Release notes" title="Run failed">
        <Badge label="Step 6 of 9" />
        <Text text="The publisher agent could not reach the wiki." />
        <Text text="Failed 3 times, then stopped the run." role="helper" />
      </Panel>
      <Panel title="What you can do">
        <List>
          <ListItem label="Re-queue from step 6" meta="keeps earlier output" />
          <ListItem label="Open step 6" meta="check the target folder" />
          <ListItem label="Cancel the run" meta="discards the draft" />
        </List>
      </Panel>
      <Stack gap="sm">
        <Button label="Re-queue from step 6" emphasis="primary" navigateTo="runs" />
        <Button label="Open the step" navigateTo="step-config" />
        <Button label="Back to runs" emphasis="tertiary" navigateTo="runs" />
      </Stack>
    </Stack>
  </Screen>
</Wireframe>

## What the sketch is claiming

Three claims are worth arguing with before anything is built.

A workflow is edited on a canvas, not in a list, because an author reasons about order and branching visually.

A gate is a step like any other, so approval shows up in the same run timeline as the work it is holding up.

A run is the unit a person opens when something goes wrong, which is why the phone screen is a failed run rather than a dashboard.
