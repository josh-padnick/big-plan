# Review Storage Decisions

This excerpt captures the weighty choices for persisting plan-review threads and delivering rendered artifacts.
Each decision carries enough tradeoff context to be judged on its own.

## Persistence layer

<BigDecision question="Which persistence layer should back review comments?" status="open" reversibility="Moderate. The repository layer isolates SQL, so swapping engines later costs a data migration, not a rewrite.">

Comments need durable identities, ordered replies, and range anchors that survive a renderer restart.
The first release runs locally, but the data model should not block concurrent reviewers later.

<Criterion title="Anchor integrity">

Selection anchors and their threads must stay consistent through crashes and concurrent writes.

</Criterion>

<Criterion title="Local-first setup" />

<Criterion title="Concurrent reviewers later" />

<Criterion title="Operational tooling" />

<Option title="PostgreSQL" recommended summary="The relational store the team already operates.">

<Score criterion="Anchor integrity" verdict="Strong" tone="good">

Transactions keep a thread and its selection anchor in one atomic write.

</Score>

<Score criterion="Local-first setup" verdict="Needs a server" tone="bad" />

<Score criterion="Concurrent reviewers later" verdict="Ready" tone="good" />

<Score criterion="Operational tooling" verdict="Mature" tone="good">

Backup, migration, and observability tooling exists from day one.

</Score>

The initial schema needs only plans, threads, comments, and immutable selection anchors.
Presence and notification state stay out of scope.

</Option>

<Option title="SQLite" summary="One embedded database beside the local server.">

<Score criterion="Anchor integrity" verdict="Strong" tone="good" />

<Score criterion="Local-first setup" verdict="Zero setup" tone="good" />

<Score criterion="Concurrent reviewers later" verdict="Single writer" tone="mixed">

A known migration point arrives before shared review can scale.

</Score>

<Score criterion="Operational tooling" verdict="File-based" tone="neutral" />

</Option>

<Option title="JSON event log" summary="One append-only file per workspace.">

<Score criterion="Anchor integrity" verdict="App-enforced" tone="bad">

Compaction, corruption recovery, and concurrent writes all become application code.

</Score>

<Score criterion="Local-first setup" verdict="Zero setup" tone="good" />

<Score criterion="Concurrent reviewers later" verdict="Not viable" tone="bad" />

<Score criterion="Operational tooling" verdict="Plain files" tone="neutral" />

</Option>

</BigDecision>

## Artifact delivery

<BigDecision question="Where should rendered plan artifacts live by default?" status="decided" reversibility="Cheap. A flag flip changes the default; no stored data moves.">

The output should be easy to find after a CLI render and remain explicit when automation needs a different destination.

<Criterion title="Discoverability" />

<Criterion title="Clean source tree" />

<Option title="Alongside the source plan" chosen recommended summary="HTML lands next to the MDX input unless a path is given.">

<Score criterion="Discoverability" verdict="Immediate" tone="good" />

<Score criterion="Clean source tree" verdict="Adds output files" tone="bad" />

</Option>

<Option title="Tool-owned cache directory" summary="Artifacts collect under Big Plan's local cache.">

<Score criterion="Discoverability" verdict="Needs a lookup" tone="bad">

Artifacts can also outlive the source version that produced them.

</Score>

<Score criterion="Clean source tree" verdict="Untouched" tone="good" />

</Option>

</BigDecision>

## Reviewer reminders

<BigDecision question="How should reviewers receive decision reminders?" status="deferred" reversibility="Free until built. Nothing ships in this delivery either way.">

Notifications depend on reviewer identity and a live session model that this static delivery does not yet own.
The alternatives remain recorded so the live-review plan can resume from an explicit boundary.

<Criterion title="Reaches a known person" />

<Criterion title="Ships without new infrastructure" />

<Option title="Wait for signed-in identities" recommended summary="Route reminders once a live session knows the reviewer.">

<Score criterion="Reaches a known person" verdict="Yes" tone="good" />

<Score criterion="Ships without new infrastructure" verdict="Deferred" tone="neutral" />

</Option>

<Option title="Browser notifications" summary="Alert the local reviewer from the page itself.">

<Score criterion="Reaches a known person" verdict="Tab only" tone="bad" />

<Score criterion="Ships without new infrastructure" verdict="Yes" tone="good">

Permission prompts arrive before Big Plan can explain which events matter.

</Score>

</Option>

</BigDecision>
